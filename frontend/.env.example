# Production / Remote Backend URL
VITE_API_BASE_URL=http://localhost:8000

# Optional: WebSocket endpoint override (defaults to ws(s) based on VITE_API_BASE_URL)
# VITE_WS_BASE_URL=ws://localhost:8000
